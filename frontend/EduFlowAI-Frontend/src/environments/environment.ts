// environment.ts
export const environment = {
  production: true,
  // Empty string enables same-origin relative /api calls routed via CloudFront for zero-CORS latency & HTTP/2 speed
  apiUrl: ''
};

